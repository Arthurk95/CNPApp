<!DOCTYPE html>
<html>
  <nav><%- include header.ejs %></nav>
  <head>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/students.css' />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <script src="/javascripts/reportScripts.js"></script>
  </head>
  <body>
    <a id="test" style="visibility: hidden;"><%= report %></a>
    <% if(report.length > 0) {%>
      <% report.forEach((report, index) => {%>
        <table>
          <thead>
            <tr><% report.name %></tr>
          </thead>
          <tbody>
            <% report.listOfActivities.forEach((activity, index) => {%>
              <tr><% activity %></tr>
            <% }) %>
            <tr></tr>
          </tbody>
        </table>
      <% }) %>
    <% } else {%>
      <h2>no reports</h2>
    <% } %>
  </body>
  </html>