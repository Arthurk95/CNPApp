<!DOCTYPE html>
<html>

  <nav><%- include header.ejs %></nav>
  <head>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/students.css' />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  </head>
  <body>
        <% if (activities.length > 0) {%>
        <table>
            <thead>
                <tr>
                    <th scope="row" colspan="3" style="text-align: center;">CNP Activities</th>
                </tr>
            </thead>
            <tbody>
                <% activities.forEach((activity, index) => { %>
                    <tr>
                        <td scope="row"><%= activity.ActivityId %></th>
                        <td><%= activity.ActivityName %></td>
                        <td class="optionsColumn">
                            <a class="adminButton" value="<%= activity.ActivityId %>" id="deleteActivityButton<%= activity.ActivityId %>">Delete</a>
                            <script>document.getElementById("deleteActivityButton<%= activity.ActivityId %>").addEventListener("click", function() { httpPostAsync("deleteActivity<%= activity.ActivityId %>");}, false);</script>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } else { %>
        <h2>no activitie</h2>
    <% } %>
  </body>
  <script>
    rows = document.getElementsByTagName("tr");
    for(var i = 2; i < rows.length; i++){
        if(i % 2 == 0){
            rows[i].classList += " grayRow";
        }
    }
  </script>
</html>
